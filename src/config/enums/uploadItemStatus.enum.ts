export enum UploadItemStatus {
  UPLOADING = 'uploading',
  MERGING = 'merging',
  TRANSCODING = 'transcoding',
  COMPLETED = 'completed',
  FAILED = 'failed',
  CANCELLED = 'cancelled',
  DELETED= 'deleted',
}
